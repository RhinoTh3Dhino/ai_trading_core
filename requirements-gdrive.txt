# === Google Drive integration ===
pydrive2==1.21.3          # Google Drive API wrapper

# === SSL stack (kompatibel med pydrive2 / pyopenssl) ===
pyopenssl==24.2.1         # SSL cert håndtering
cryptography>=41.0.5,<44  # OBS: Må ikke være 44+ pga. pydrive2/pyopenssl krav

# === Netværk & HTTP-klient ===
requests>=2.31,<3.0       # HTTP requests
